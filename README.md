Maybe it's something I'm just learning
### Notes
- ConfigMap for each service
- NodePort is used for requests from the outside (Ingress, Loadbalancer)


```
kubectl apply -f {filename}.yaml
kubectl delete -f {filename}.yaml
```
